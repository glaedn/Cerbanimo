import { styled } from '@mui/material/styles';
import {
  HUDPanelContainer,
  HUDPanelHeader,
  HUDPanelTitle,
} from '../HUDPanel.styles';

export const AffinityGalaxyPanelContainer = styled(HUDPanelContainer)({
  // Add any specific styles for AffinityGalaxyPanel here
});
