import { styled } from '@mui/material/styles';
import {
  HUDPanelContainer,
  HUDPanelHeader,
  HUDPanelTitle,
  HUDPanelList,
  HUDPanelListItem,
} from '../HUDPanel.styles';

export const TargetingScannerContainer = styled(HUDPanelContainer)({
  // Add any specific styles for TargetingScanner here
});

export const TargetItem = styled(HUDPanelListItem)({
  // Styles for individual target items
});
